apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  monasca.grafana.local.password: "bG9jYWw="
  monasca.grafana.aes.key: "QVZlcnlIYXJkUGFzc3dvcmQ1ITEyMzQ1Njc4OTBBQkM="
# grafana secret - names required in this way, so that grafana can use the env variable right away
  gf.security.admin.password: "bG9jYWw="
